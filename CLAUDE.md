# Equestrian Coaches Australia

## What this is

A new online marketplace/directory business connecting riders with riding coaches and instructors across Australia. Riders search by discipline and location; coaches create paid profiles to be found. Think a niche, higher-style version of a classifieds/directory site — closer in spirit to [Horse Deals](https://www.horsedeals.com.au/) functionally, but with a more premium, editorial design treatment rather than a classifieds look.

This is a pre-launch venture — no existing site, brand assets, or codebase yet. This file (and this Cowork project folder) is the source of truth for the concept as it develops.

## Core concept

- **For riders:** search and browse coaches by discipline and location, free to use. No login required to browse (assume optional accounts for saving favourites/notifications later).
- **For coaches:** pay a monthly subscription to list a profile. Coaches upload their own info — description/bio, photo, location, specialties, qualifications, testimonials — and tag themselves under one or more discipline categories.
- **Matching model:** riders find "the perfect coach in the exact discipline they're looking for" — the whole value proposition rests on precise discipline + location filtering, not a generic instructor list.

## Two-sided marketplace, one-sided pricing

- **Riders:** free, always. No revenue from the rider side at launch.
- **Coaches — two tiers:**
  - **Standard listing — $9.99/month AUD.** Profile with bio, photo, location, specialties, qualifications, testimonials, discipline category tags.
  - **Standard + Clinics/Events — $14.95/month AUD.** Everything in Standard, plus the ability to list clinics and events (e.g. a coach hosting a weekend dressage clinic).
- Pricing is indicative/target pricing for launch, not yet finalised or tested with real coaches.

## Discipline categories (launch scope)

Coaches tag their profile with the discipline(s) they teach so riders can filter accurately. Named examples so far: **Western, Dressage, Liberty**. This list is expected to grow (e.g. showjumping, eventing, campdrafting, natural horsemanship, pony club, para-equestrian) — the category taxonomy should be built to be extensible, not hard-coded to 3.

## Search & discovery

Primary rider use case: *"find a dressage instructor near me"* → see all coaches in that discipline, ranked/filtered by proximity. So the search experience needs, at minimum:
- Filter by discipline (multi-select ideally, since coaches can teach more than one)
- Location-based search (postcode/suburb/region, with a "near me" or radius option)
- Coach profile cards showing enough to compare at a glance: photo, name, discipline tags, location, and probably a headline/short bio snippet

## Notifications (aspirational feature, not launch-critical)

Ideally riders can opt in to notifications like: *"Jane Smith is hosting a dressage clinic in your area."* This implies:
- Riders need some way to express "my area" and "my disciplines of interest" (even without a full account system) for this to be targetable
- Ties directly to the $14.95 coach tier, since only those coaches can post clinics/events
- Treat as a v1.x/v2 feature to design around, not necessarily to build first

## Roadmap beyond coaches

The long-term vision is broader than riding coaches — the plan is to eventually expand the same directory/marketplace model to adjacent equine service providers: **bodyworkers, equine chiropractors, farriers**, etc. Implication for design/architecture: the category and profile system should be modelled generically enough (e.g. "service provider" as a concept, with "riding coach" as the first provider type) that new provider types can be added later without a rebuild. Launch scope is riding coaches only — do not build out the other provider types yet, just don't design it into a corner.

## Competitive reference

- **[Horse Deals](https://www.horsedeals.com.au/)** — Australia's long-running equestrian classifieds marketplace (since 1986, now online). Cited by the client as the closest functional comparison (marketplace/classifieds mechanics, equestrian audience) but the brief explicitly wants **more style/polish** than Horse Deals' current look — a more premium, editorial feel rather than a classifieds aesthetic.

## Design direction (from initial brief discussion)

- Feel: **premium & editorial**, blended with **warm & community-led** — polished and aspirational, but still approachable and about real people (coaches and riders), not cold or corporate.
- Audience: two distinct groups — riders (browsing/searching, free, casual) and coaches (professionals managing a paid profile, want to look credible and be found). Skews adult, likely majority female, broad age range across the recreational-to-competitive riding community.
- No existing brand assets (logo, colours, fonts) yet — this is being defined through the design exploration process.
- Working toward: 5 distinct visual directions generated via Claude Design, to compare and choose from before committing to a build.

## Competitive landscape (research done 27 Aug 2026)

Full writeup: `Competitor-Landscape.docx` in this folder. Headline findings:

- **No direct competitor exists** — nobody in Australia runs a dedicated, subscription-based, cross-discipline coach directory. The opportunity is real.
- **Equestrian Australia (EA) "Coach Finder"** is the official governing-body register — free, trusted, but only lists EA-accredited coaches and skews to FEI/Olympic disciplines (dressage, jumping, eventing, para). Western, Liberty and similar disciplines generally fall outside EA's accreditation pathway — a real gap ECA can fill without needing to compete with EA's authority directly.
- **EquiDirectory.com.au** is the closest functional match: a 50+ category general equine business directory that already has a "Coaching / Riding Lessons" category, plus real estate/breeding/saddlery categories that already prove out the "expand beyond coaches later" instinct. It monetises via ad-hoc per-listing/per-year fees ($80–$400 range) rather than a clean subscription, and its design is dated/cluttered — both are openings for ECA.
- **Horse Deals and MyAusHorse** both bolt a generic "business directory" onto their classifieds, with coaching as one category among many (not a dedicated searchable profile experience).
- **Pony Club Australia** is youth/club-focused with volunteer instructors — adjacent, not competing.
- Booking/scheduling SaaS (Vagaro, Bookeo, etc.) are back-office tools some coaches may already use — worth a future booking integration, not a discovery competitor.
- Differentiation to lean into: no gatekeeping by discipline/accreditation, a product wholly dedicated to coach discovery (not one category of many), flat simple pricing, materially better design/craft, and clinic/event notifications as a paid-tier feature nobody else offers.

## Payments & billing (research done 27 Aug 2026)

Build platform decision: custom-coded (via Claude Code), not a no-code site builder — so any payment provider with a proper API/SDK works. This was checked against Wix/Squarespace (27 Aug 2026) and confirmed: the product needs self-serve coach-editable profiles, structured discipline+location search across many listings, and feature-gated subscription tiers — a combination neither builder handles natively (Squarespace's Member Areas only gate whole pages/content by plan, not fine-grained features; Wix could technically get there via its Velo custom-code layer, but that's writing real code inside a platform with its own data/hosting ceilings and lock-in, not a no-code shortcut). Custom code wins on all three of quickest/cheapest/most flexible for this specific product shape — see chat for the full reasoning. Wix/Squarespace would still be the fast, cheap right answer for a simple waitlist/landing page if one's wanted before the full app is ready.

Money only flows one direction at launch: coach → platform (the $9.99/$14.95 monthly subscription). Riders never pay, so there's no need for split/marketplace payouts (Stripe Connect etc.) yet — plain recurring subscription billing is enough. Revisit this only if a future feature has riders paying coaches directly through the platform.

Recommendation: **Stripe** (Checkout + Subscriptions/Billing + Customer Portal), for AUD-native support, cheapest domestic-card rate of the options compared, and by far the best-documented option for an AI-assisted custom build (Customer Portal gives free self-serve plan management/cancellation, so it doesn't need to be hand-built).

Fee snapshot per provider (approx. cost on a $9.99 charge, AU domestic card unless noted):

- Stripe: 1.7% + A$0.30 domestic card (confirmed effective 1 Oct 2026 per Stripe's AU pricing page) ≈ $0.47 (~4.7% of a $9.99 charge)
- Square: 2.2% flat, no fixed fee ≈ $0.22 (~2.2%) — actually cheaper than Stripe at this low ticket size specifically because there's no fixed per-transaction fee, though Stripe's docs/SDKs and Customer Portal are the bigger factor for build speed
- GoCardless (BECS direct debit): 1% + $0.40 (capped $4) ≈ $0.50 (~5%) — bank debit clears in 2–3 business days, so provisioning logic needs to account for delayed confirmation, not instant like a card
- Paddle / Lemon Squeezy (Merchant of Record): 5% + $0.50 ≈ $1.00 (~10%) — much pricier per-transaction, but they become the legal seller and handle all sales-tax/GST compliance for you; worth revisiting if/when expanding beyond Australia

Important regulatory note: the RBA's card surcharge ban takes effect 1 October 2026 (eftpos/Mastercard/Visa) — you will not be able to pass the processing fee back to coaches as a checkout surcharge, so whichever provider's fee applies comes straight out of the $9.99/$14.95 revenue. Factor that into margin, not into pricing display.

GST: flag to an accountant/bookkeeper before launch — Stripe Tax exists to automate this (percentage or flat per-transaction cost) but for a single-country, single 10%-rate business it may be simpler to just build the 10% GST into the listed price manually rather than pay for Stripe Tax at this stage.

## Tech stack (decided 27 Aug 2026, hosting revised 28 Aug 2026)

- **Frontend:** Next.js (React) — not a plain client-side React SPA, specifically because coach profile pages need to be server-rendered/crawlable for Google to index "dressage coach near [suburb]" style searches, which is a real organic acquisition channel for riders.
- **Hosting:** **Vercel** — reversing the earlier 27 Aug Cloudflare Pages decision. Vercel is Next.js's native platform (zero-config deploys, no community adapter needed), and this project already has Vercel tooling/skills wired into the build environment. The commercial-use restriction on Vercel's free Hobby tier (noted in the original 27 Aug research) means launch will need at least the Pro plan (~US$20/mo) once the site is live and monetising — budget for that from day one rather than treating it as a "later" upgrade. Cloudflare Pages remains a fallback if Vercel cost/limits ever become a problem.
- **Backend/DB/Auth/Storage:** Supabase (their existing tool of choice) — Postgres with the **PostGIS** extension enabled for real "coaches within X km" radius search, built-in Auth with row-level security for separate coach/rider accounts, Storage for profile photos, Edge Functions for Stripe webhook handling. **Free tier ($0/mo) is genuinely fine at launch**: 500MB DB, 50k monthly active users, 1GB file storage, 5GB egress — no commercial-use restriction, only resource caps and a pause after 1 week with zero activity (a non-issue once real users are hitting it). Compress/resize coach photos on upload (e.g. ~800px wide) to make the 1GB storage cap stretch to thousands of profiles. Upgrade to Pro (US$25/mo) only when nearing these limits — a handful of paying coaches already covers that.
- **Payments:** Stripe (see Payments & billing section above).
- **Geo search implementation note:** don't pay for a geocoding API on every search — Australia has free/cheap static postcode-to-lat/long datasets (e.g. via data.gov.au) that can be loaded once into a lookup table, then PostGIS handles the radius query.
- **Notifications:** Real push notifications without an app store install ARE possible — Web Push via a installable PWA (web app manifest + service worker) — but iOS Safari only delivers push to a PWA a user has manually "Added to Home Screen"; it does not work for a normal browser tab visit, and there's no workaround (Apple platform restriction). Given most riders won't proactively install anything, plan email as the reliable primary notification channel (e.g. via Resend/Postmark, cheap transactional email) with web push as a bonus layer for engaged users who do install it. Revisit a native app only if real usage data shows push adoption/demand justifies the extra build and app-store overhead.
- **Estimated infra cost:** **~US$20/month to start** (Vercel Pro, required for commercial use) + Supabase free tier ($0/mo until traffic/storage nears its limits) + a ~$15–20/year domain + Stripe's per-transaction fees only (which scale with actual revenue). Upgrade Supabase to Pro (US$25/mo) only once nearing its free-tier limits — by which point coach subscriptions already cover it many times over.

## Auth: Clerk vs Supabase Auth (assessed 27 Aug 2026)

Claude Code suggested Clerk during the build. Assessed and decided: **stick with Supabase Auth**, not Clerk, at this stage.

- Supabase Auth costs nothing extra — it's already bundled into the Supabase plan we're already on (free tier now, Pro later), and it's natively wired into the same Postgres row-level-security rules the rest of the app needs, so there's no second identity system to keep in sync.
- Clerk's free tier (50,000 monthly retained users) explicitly excludes MFA, passkeys, and — the one that matters most here — **removing Clerk's own branding** from the sign-in/sign-up screens. Getting an unbranded auth experience that matches the premium design work means Clerk's Pro plan, US$20–25/month minimum, plus $0.02 per additional user beyond 50k (vs Supabase's own $0.00325/MAU overage — about 6x pricier per extra user).
- Confirmed via Supabase's own docs/blog: if Clerk were ever used, Supabase does support it as an official "third-party auth" provider (Clerk issues the JWT, Supabase trusts it directly for RLS — a real, supported integration, not a hack) and doesn't double-charge Supabase-side auth fees on top of Clerk's own subscription. So the integration itself isn't the problem — the cost is paying Clerk at all for what Supabase Auth already does for free.
- Clerk's genuine advantages (prebuilt polished components, passkey/WebAuthn support, an "Organizations" primitive for a future multi-coach "riding school" account type) are real, just not worth $20–25/mo yet: the components need custom restyling either way to match the chosen design direction, and Supabase Auth already includes MFA (TOTP) for free.
- Revisit Clerk specifically if: passkey login becomes a priority, a multi-seat "organization" account type is needed, or hand-building/maintaining auth screens becomes a genuine recurring time sink.

## App build (started 28 Aug 2026)

The real app is being built ahead of a chosen visual direction, skinned generically so a direction can be applied as a design-token swap later. Full plan lives in this session's approved plan (repo root is now the Next.js app; the design deck moved to `design-preview/`, still live via a `gh-pages` branch). MVP scope: rider search/browse, coach profile CRUD, Stripe subscription billing (both tiers), clinics/events full CRUD, rider accounts with favourites + clinic email notifications — mobile-first throughout.

Build phases: 1 Scaffold, 2 Data+Auth (Supabase), 3 Coach profile CRUD, 4 Search & discovery, 5 Payments (Stripe), 6 Clinics + notifications (Resend), 7 Rider account, 8 Polish/SEO/QA.

- [x] **Phase 1 — Scaffold:** Next.js (App Router, TS, Tailwind v4) at repo root; generic CSS-variable design tokens in `src/app/globals.css` (neutral warm palette, swap when a direction is chosen); base layout with mobile-first header (hamburger nav) + footer; placeholder discipline/coach data (`src/lib/`) standing in for Supabase tables; UI-only pages for the full site map (home, search, discipline pages, coach profile, for-coaches/pricing, login/signup, rider account, coach dashboard incl. profile/clinics/billing tabs) — no backend wired yet.
- [x] **Phase 2 — Data + Auth:** live Supabase project connected (`.env` has real keys, gitignored — not committed) and all three migrations run successfully — schema (profiles, disciplines, coach_profiles w/ PostGIS location, coach_disciplines, coach_photos, testimonials, clinics with tier-gated RLS, favourites, rider_preferences, notifications_log, postcodes), storage bucket, and discipline seed are all live. `src/lib/supabase/{client,server,middleware}.ts` set up per the official `@supabase/ssr` pattern; `src/proxy.ts` refreshes sessions and gates `/dashboard` (coach-only) + `/account` (auth-only). Login/signup wired to real `supabase.auth` calls (email confirmation via `/auth/callback`, sign-out route). Verified end-to-end via a real signup attempt against the running app.
- [x] **Phase 3 — Coach profile CRUD:** `/dashboard/profile` is fully wired — server actions in `src/app/dashboard/profile/actions.ts` handle profile save, photo upload/delete (Supabase Storage, `coach-photos` bucket, RLS keyed to `<coach_id>/...` path prefix — see `supabase/migrations/0002_storage.sql`), discipline tagging, and testimonial add/delete. Coach row is lazily created on first dashboard visit (`ensureCoachProfile` in `src/lib/supabase/queries.ts`) rather than at signup. Dashboard overview now shows the real subscription/published status. Discipline taxonomy reads from the DB with a static fallback (`getDisciplines`) so it works before/after the migration is run.
- [x] **Phase 4 — Search & discovery:** real discipline + radius search is live. `postcodes` table loaded with all ~18.5k AU postcode/suburb/lat-long rows (`supabase/scripts/load-postcodes.mjs`, sourced from the [matthewproctor/australianpostcodes](https://github.com/matthewproctor/australianpostcodes) open dataset — raw CSV kept local/gitignored under `supabase/data/`, re-download from the script's header comment if needed). `resolveLocation()` turns free text ("Bendigo VIC", "3550", "Toowoomba") into a lat/long via that table; `nearby_coaches()` (`supabase/migrations/0003_search.sql`) is a Postgres RPC doing the actual PostGIS `ST_DWithin`/`ST_Distance` radius query, discipline-filtered and published-only. `searchCoaches()` in `src/lib/supabase/queries.ts` calls the RPC then hydrates results with name/disciplines/photo for `CoachCard`. Home, `/search`, `/disciplines/[slug]` and `/coaches/[slug]` all read real data now (still falling back to placeholder content if Supabase isn't configured). Coach profile save now geocodes suburb/postcode into `coach_profiles.location` so newly-edited profiles are searchable. Verified with a full smoke test (throwaway published coach via the GoTrue admin API + direct DB insert, confirmed it surfaces correctly in `/search` and `/coaches/[slug]` in a live browser, then deleted).
- [x] **Phase 5 — Payments, running in mock mode by design (29 Aug 2026):** who actually owns/holds the Stripe account is still an open business-structure question, so real Stripe is deliberately deferred rather than blocked-on. `src/lib/stripe.ts` exports `isMockPayments` (`= !isStripeConfigured`) alongside the real `getStripe()`/`isStripeConfigured`. `src/app/dashboard/billing/actions.ts`: `startCheckout` branches — mock mode writes the same `coach_profiles` fields a real webhook would (`subscription_tier`, `subscription_status: 'active'`, `published: true`, a fake `stripe_customer_id`) and redirects straight to `/dashboard?checkout=success&mock=1`, no card, no Stripe API call; real mode (once keys exist) creates/reuses a Stripe customer and a Checkout session, unchanged from the original design. `mockCancelSubscription` stands in for the Customer Portal's cancel action in mock mode. `src/app/api/webhooks/stripe/route.ts` is untouched and ready for when there's a real account — publishing still only ever happens there for the real path, never on the Checkout redirect. UI (`/for-coaches`, `/dashboard`, `/dashboard/billing`) shows a plain-language "running on mock payments" notice wherever billing is mentioned, and every button is fully live (no more `disabled` states) since mock mode makes them actually work. **Nothing else in the app needs to change when a real Stripe account is finally created** — dropping real keys into `.env` flips `isMockPayments` to `false` and the exact same UI starts hitting real Stripe instead.
  - Bug fixed this phase: `startCheckout`/`openBillingPortal` assumed a `coach_profiles` row already existed, but that row is only lazily created on a coach's first visit to `/dashboard/profile` — a coach going straight to billing hit a silent no-op UPDATE (0 rows, no error) and the "mock subscription activated" banner showed while the DB stayed unsubscribed. Fixed by calling `ensureCoachProfile` in `requireCoach()` so every billing action guarantees the row exists first. Caught by a real smoke test, not by inspection — same lesson as phase 4: don't trust "no errors shown", read the actual DB row back.
  - Also hit and worked around: this session's long-lived Browser-tool tab accumulates stale Supabase auth cookies across smoke tests (a previous test's now-deleted user's session lingers and causes 403/406s and phantom "logged in" header state). Fix for future smoke tests: either open a fresh tab **and** clear `document.cookie` before logging in as a new throwaway user, or explicitly sign out first — a new tab alone doesn't help since cookies are per-origin, not per-tab.
  - Also: clicking is unreliable when the Browser pane isn't actually displayed to the user (`screenshot`/`computer` calls fail with "pane is not displayed" but don't always error on click) — when that happens, submit forms via `document.querySelector('form').requestSubmit()` through `javascript_tool` instead of `computer` clicks, and verify the result via `get_page_text`, not by assuming the click landed.
- [x] **Phase 6 — Clinics + notifications:** full clinics CRUD at `/dashboard/clinics` (+ `/dashboard/clinics/[id]/edit`), gated at both the app level (`requireClinicsTierCoach` in `src/app/dashboard/clinics/actions.ts`) and the DB level (the existing RLS policy on `clinics`). Public `/clinics/[id]` page, linked from both the coach profile's "Upcoming clinics" and the dashboard list. Matching: `supabase/migrations/0006_notify_matching.sql`'s `matching_riders_for_clinic()` RPC finds riders who either follow the clinic's discipline or sit within 100km of the coach, excluding anyone already in `notifications_log` for that clinic (dedupe). `src/lib/notifications.ts`'s `notifyRidersOfClinic()` runs that RPC under the service-role key, emails each match via Resend (`src/lib/resend.ts`, same `isResendConfigured`-guarded pattern as Supabase/Stripe — with no key set it just logs the match to `notifications_log` and skips sending, so matching is fully testable before Resend exists), and is fired inline from `createClinic` right when a clinic is saved. `/api/cron/notify-clinics` is a backstop for the case a rider sets preferences *after* a clinic already exists — meant to run daily via Vercel Cron (`vercel.json`, not active until deployed to Vercel), protected by `CRON_SECRET`. Pulled one small piece of phase 7 forward out of necessity: `/account`'s "notify me" form is now fully wired (`src/app/account/actions.ts`) since notifications need somewhere real to match against — favourites are still not. `profiles` gained an `email` column (mirrored from `auth.users` at signup, since PostgREST doesn't expose the `auth` schema) so the matcher can email people without an admin-API round-trip per rider. Verified with two smoke tests: a DB-level one proving the RPC matches correctly and the dedupe actually excludes already-notified riders, and a full browser UI pass (create → appears in dashboard list, public clinic page, and coach profile → delete), both cleaned up after.
- [x] **Phase 7 — Rider account:** favourites are the last piece (preferences were pulled forward into phase 6). `src/app/coaches/actions.ts`'s `toggleFavourite()` is called directly from `src/components/favourite-button.tsx`, a small client component on `/coaches/[slug]` that checks auth + current favourite status on mount and sends a logged-out click to `/login?next=/coaches/[slug]` rather than erroring. `/account` now lists real favourites (name, location, headline, linked to the profile) with a working remove button (`removeFavourite` in `src/app/account/actions.ts`). Caught one React 19 lint rule this phase — `react-hooks/set-state-in-effect` flags a synchronous `setState` in an effect body; fixed by deriving the "Supabase not configured" case from `isSupabaseConfigured` in the initial `useState` instead of setting it inside the effect. Verified live: favourited a coach as a throwaway rider, confirmed the button flipped to "♥ Favourited" with no reload, confirmed it appeared on `/account`, removed it there, confirmed it's gone — then also independently verified the notify-me preferences save (pulled forward last phase) actually persists to the DB (suburb/postcode/geocoded point/discipline all correct), not just that the UI didn't show an error. Both throwaway accounts deleted after.
- [x] **Phase 8 — Polish/SEO/QA:** root layout gained `metadataBase`, Open Graph/Twitter card metadata, and a default `robots` policy; every dynamic page (`/search`, `/for-coaches`, `/disciplines/[slug]`, `/coaches/[slug]`, `/clinics/[id]`) has a real per-page description, not just a title. `/dashboard/*` and `/account` are `robots: noindex` (private, auth-gated). `src/app/sitemap.ts` lists static routes + all 11 discipline pages + real published coaches only — deliberately excludes mock data and the static placeholder fallback, since those aren't real listings. `src/app/robots.ts` points crawlers at it and disallows the private/auth routes. Added a branded `not-found.tsx` (was the plain Next.js default) and a "Skip to content" link + `id="main-content"` landmark on the root layout for keyboard/screen-reader users. Spot-checked the placeholder colour tokens' contrast ratios by hand (muted-on-bg ≈5.3:1, accent-on-bg ≈6.9:1, fg-on-bg ≈15:1, danger-on-bg ≈5:1) — all clear AA, no changes needed; worth re-checking once a real palette replaces the placeholder tokens. Verified live: `robots.txt`/`sitemap.xml` render correctly, custom 404 triggers on an unknown coach slug, no horizontal overflow at 375px on home/search/for-coaches/coach-detail/login, mobile hamburger menu opens cleanly, skip link and main landmark both present in the DOM.
  - Not done, and flagged rather than silently skipped: no automated accessibility audit tool was run (e.g. axe) — this was a manual/targeted pass, not exhaustive. No image `alt` text audit was needed since the app has no real `<img>` content yet (coach photos come in phase 3's upload flow but no coach has uploaded one in testing) — revisit once real photos exist. Favicon is still the Next.js default — cosmetic branding is blocked on the chosen design direction, tracked below under Status/next steps.
- [x] **Phase 9 — Taxonomy (per the build spec above, 29 Aug 2026):** replaced `disciplines`/`coach_disciplines` with a general `terms`/`coach_terms` system covering three kinds — discipline, skill, attribute — so the alias layer, admin UI and search filter only get built once.
  - **Migrations** (`0007`–`0011`): `terms` (kind enum, slug, name, `generates_pages`, `active`) + `term_aliases` (`unique(lower(alias))`, `is_primary`, `source`) + `coach_terms` (`sort_order` — lowest = primary discipline) + `term_suggestions` + `term_slug_history` (table exists, no redirect logic yet — see deferred list) + `admin_users`/`is_admin()` (deliberately its own table, not a `user_role` enum value, so granting admin never touches the signup trigger or forces a choice between admin and coach). `0008` migrates existing data via insert-select (UUIDs preserved) and seeds 8 new disciplines (trail-riding, showing-hack, polocrosse, endurance, stock-horse, mounted-games, vaulting, carriage-driving — 19 total), 72 aliases, 25 skills, 22 attributes, 53 curated discipline→skill/attribute suggestions. `0009` adds `areas` (suburb/town/region/state + centroid), mechanically populated from the 18.5k `postcodes` (17,515 distinct areas, 100% mapped). `0010` rewrites `nearby_coaches` for multi-select (`p_discipline_ids`/`p_skill_ids`/`p_attribute_ids` arrays, OR within a kind/AND across kinds, `match_count` returned for tie-breaking after distance). `0011` adds `search_events` (service-role insert only, no anon policy).
  - **App rewired end-to-end:** `getTerms`/`getDisciplines`/`getSkills`/`getAttributes`/`getSuggestedTerms` in `src/lib/supabase/queries.ts` (static fallback still covers disciplines pre-migration); `searchCoaches` takes a `SearchFilters` object; every `coach_disciplines`/`disciplines` reference across search, discipline pages, coach detail, clinic pages, the profile editor and its save action now reads/writes `coach_terms`/`terms`. Profile editor (`src/app/dashboard/profile/profile-form.tsx`) has three checkbox groups (disciplines/skills/attributes) sharing one `selectedTermIds` array; save action assigns `sort_order` from checked order so the first discipline ticked leads the coach's page title. `/search` takes a comma-separated `d` param (was single-value `discipline`), multi-select via `src/components/discipline-filter.tsx`, and is now `robots: noindex, follow` per the spec. `src/lib/search-events.ts` logs every search server-side via a service-role client (never breaks a search on logging failure). Minimal `/admin/terms` (rename, deactivate/reactivate, toggle `generates_pages`, add) and `/admin/aliases` (add/remove) screens, gated by `is_admin()` at both `src/lib/supabase/middleware.ts` and a server-side re-check in `src/app/admin/layout.tsx`. First admin has no UI — grant via `insert into admin_users (user_id) values (...)` directly, per the spec's own "can wait" note.
  - **Bugs caught and fixed before/during migration, not after:** `clinics.discipline_id`'s FK pointed at `disciplines`, which `0008` drops — repointed the constraint to `terms` immediately before the drop, relying on discipline UUIDs being preserved by the insert-select. The `areas` population hit two real-world slug collisions (case/whitespace variants of the same suburb name, e.g. two `k-gari-qld` rows) — fixed by grouping on `lower(trim(suburb))` instead of raw `suburb`, plus `on conflict (slug) do nothing` as a backstop (first one wins; rare enough not to warrant a fussier slug algorithm at seed time). Both areas-migration failures left partial DDL committed (statements aren't wrapped in one transaction) and needed manual `drop table`/`drop type` cleanup before re-running the corrected file — same non-transactional-script caveat as earlier phases, worth remembering for any future multi-statement migration.
  - **Also caught my own mistake, not the app's:** while smoke-testing the admin aliases "Remove" button, a DOM query (`textContent.includes(...)` on a container div, then grabbing its first `<form>`) matched the wrong row and deleted a different seed alias than intended. Caught by checking the actual row count in the DB afterwards (stayed at 72 instead of dropping), not by trusting the re-rendered UI — restored via direct SQL. Reinforces the standing lesson already in this file: verify against the database, not against "no error was shown."
  - Verified live end-to-end with two throwaway accounts (admin + coach, both deleted after): admin terms/aliases screens render and mutate correctly; coach profile save persists correct `coach_terms` rows (right `sort_order`, right kinds) and a correctly geocoded location; `/search?d=dressage,show-jumping&location=Bendigo+VIC` OR-matches across two disciplines, sorts by distance with the real coach first at 0km, and correctly merges matching mock coaches; a `search_events` row logs with the right `term_ids`/`location_text`/`result_count`; `/disciplines/trail-riding` (one of the 8 new disciplines) renders its full page correctly with the right empty state; `/coaches/[slug]` renders discipline tags correctly off the new `coach_terms`/`terms` embed. Final `npm run build` and `npm run lint` both clean after all changes.
  - **Deferred, not silently dropped** — matches the spec's own "can wait" items plus what genuinely needs its own pass: structured data (`Person`/`LocalBusiness`, `ItemList`, `BreadcrumbList`); `term_slug_history` read/write + 301 redirect middleware (table exists, no logic built); the Search Console service account + weekly SEO digest; true drag-to-reorder for a coach's disciplines (currently just "order ticked = `sort_order`", which works but isn't the intended UI).
- [x] **Phase 9b — `indexable_pages` register + area routes (29 Aug 2026):** the two page types the taxonomy spec calls for beyond a plain discipline page — `/riding-instructors/[area]` (all disciplines, catches "riding instructor near me") and `/disciplines/[slug]/[area]` — gated by the spec's own hard rule: **never build a page with nothing real on it**, minimum 3 real coaches or redirect to a parent, never 404.
  - **`supabase/migrations/0012_indexable_pages.sql`:** `indexable_pages` table (`page_type` enum `area`/`discipline_area`, `area_id`, nullable `discipline_id`, computed `slug`, `coach_count`, `eligible`, `last_coach_change`) with two **partial** unique indexes (one where `discipline_id is null`, one where it isn't) rather than one plain unique constraint — Postgres treats `null != null`, so a bare `unique(area_id, discipline_id)` would let duplicate area-only rows pile up. `recompute_indexable_pages()` does a full `delete` + reinsert each run rather than incremental upserts — simpler, and it can never leave a stale `eligible` row behind after a coach unpublishes or moves; at launch scale (a few hundred coaches) the cost is trivial. Critically, both inserts are driven by an **inner join outward from real `coach_profiles` rows** (coach → area, coach → discipline), never a cross join of every area × every discipline — a `17,515 areas × 19 disciplines` cross join would recreate the exact 332k-doorway-page mistake the spec warns against. Same migration adds `coach_profiles.area_id` (backfilled from the existing suburb+state text via the same normalisation `0009_areas.sql` used to build `areas.slug`).
  - **`0013_areas_latlong.sql`:** added plain `lat`/`long` float columns to `areas` (same pattern as `postcodes`) — PostgREST can't apply `ST_X`/`ST_Y` to a geography column in a plain select, and the new routes need a centre point to hand straight to `searchCoaches()`.
  - **App wiring:** `resolveLocation()` (`src/lib/supabase/queries.ts`) now also returns `area_id`; `saveProfile` (`src/app/dashboard/profile/actions.ts`) persists it alongside `location` on every save, so `coach_profiles.area_id` stays current without a separate backfill step. `src/app/riding-instructors/[area]/page.tsx` and `src/app/disciplines/[slug]/[area]/page.tsx` both: look up the area, read the matching `indexable_pages` row, and redirect if `!eligible` — the area route falls back to `/search?location=...` (there's no area hierarchy above suburb yet, so no real "parent area" page exists to redirect to — a gap worth closing once regions/states get their own `areas` rows); the discipline+area route falls back to the parent `/disciplines/[slug]`, which always exists. Both merge real + mock coaches the same way `/search` and `/disciplines/[slug]` already do. `src/app/api/cron/recompute-indexable-pages/route.ts` (added to `vercel.json`, `0 3 * * *`, `CRON_SECRET`-protected, same pattern as `notify-clinics`) re-runs the recompute nightly. `src/app/sitemap.ts` now includes every `eligible = true` row from `indexable_pages` with a real `lastmod` from `last_coach_change` — never every area, only the ones that cleared the gate.
  - Verified live with three throwaway published coaches in Bendigo VIC (all tagged Dressage): `recompute_indexable_pages()` correctly produced `eligible: true` rows for both `riding-instructors/bendigo-vic` (coach_count 3) and `disciplines/dressage/bendigo-vic` (coach_count 3); both pages rendered live merging the 3 real coaches with matching mock ones; an ineligible area (`melbourne-vic`, no real coaches) correctly redirected `/riding-instructors/melbourne-vic` → `/search`; an ineligible discipline+area (`western/melbourne-vic`) correctly redirected → `/disciplines/western`; `sitemap.xml` correctly listed both eligible URLs with a real `lastmod` and none of the ineligible ones. All three throwaway coaches deleted after, confirmed `recompute_indexable_pages()` drops back to 0 rows (cascade-safe, no stale eligible pages left behind).
  - Caught in the first smoke-test pass, not the app's fault: my own test-setup script inserted `coach_profiles.area_id` directly via SQL but forgot to also set `location` (the geography point `nearby_coaches()` actually filters on) — the three real coaches silently didn't appear in results even though `eligible` was correctly `true`. `saveProfile` always sets both together from the same `resolveLocation()` call, so this can't happen through the real UI; fixed the test script and re-verified. Same standing lesson as every prior phase: read the actual result back, don't assume a green `eligible` flag means the page will render right.
- [x] **Phase 9c — Structured data (29 Aug 2026):** `Person` on coach pages, `ItemList` on every listing page, `BreadcrumbList` everywhere, per the spec.
  - **`supabase/migrations/0014_coach_latlong.sql`** adds plain `lat`/`long` columns to `coach_profiles` (same pattern as `postcodes` and `areas`) — PostgREST can't run `ST_X`/`ST_Y` on a geography column in a plain select, and `GeoCoordinates` needs a bare number. `saveProfile` now sets `lat`/`long` alongside `location`/`area_id` from the same `resolveLocation()` call, so all four always move together.
  - **`src/lib/structured-data.ts`** — the schema builders, kept out of the page components so the shape lives in one place: `coachPersonSchema()` (`Person`, not `LocalBusiness`/`Organization` — coaches on ECA are individuals, not registered businesses; that distinction is EquiDirectory's model, not this one), `itemListSchema()`, `breadcrumbSchema()`. `src/components/json-ld.tsx` renders one or more as `<script type="application/ld+json">` tags — a plain server component, no hydration needed.
  - **Wired in:** `/coaches/[slug]` gets `Person` (name, url, image, description, `jobTitle: "Riding Coach"`, `PostalAddress`, `GeoCoordinates` when lat/long resolved, `knowsAbout` from the coach's discipline names) + a 3-level `BreadcrumbList` (Home → Find a coach → coach name). `/disciplines/[slug]`, `/disciplines/[slug]/[area]` and `/riding-instructors/[area]` each get a real-hierarchy `BreadcrumbList` and an `ItemList` of the coaches actually shown (omitted entirely when there are zero, rather than an empty list).
  - Verified live: parsed every page's `<script type="application/ld+json">` tags back through `JSON.parse` in-browser (not just eyeballing markup) on a mock coach detail page (full `Person` with `geo`/`knowsAbout`/`image` populated), `/disciplines/dressage` (2-level breadcrumb + 7-coach `ItemList`), and — with three throwaway published coaches making Bendigo eligible — `/disciplines/dressage/bendigo-vic` (3-level breadcrumb) and `/riding-instructors/bendigo-vic`, both correctly merging real + mock coaches into the `ItemList`. No server errors on any page. Throwaway coaches deleted after, confirmed `indexable_pages` and `coach_profiles` both back to 0 rows.
  - **Deferred, not silently dropped:** no automated Rich Results Test / structured-data validator run (no outbound network access from this environment) — validated by parsing the JSON and checking it against the schema.org property names by hand instead. `Event` schema for `/clinics/[id]` wasn't in the spec's original list (`Person`/`LocalBusiness`, `ItemList`, `BreadcrumbList`) but would be a natural next addition given clinics are dated events with a location.
- [x] **Phase 9d — `term_slug_history` 301s, the slug trap's actual fix (29 Aug 2026):** the table has existed since `0007_taxonomy.sql`; this wires it up — a deliberate slug-change action plus middleware that 301s the old URL, so renaming a discipline's slug can never just quietly 404 an indexed page.
  - **`0015_term_slug_history_redirects.sql`:** `term_slug_history` was admin-only-select from day one (correct for writes, but the redirect check runs on every anonymous request) — added a public select policy alongside the existing admin-only `for all` policy. Nothing sensitive in an old-slug→term mapping, same reasoning as `indexable_pages`.
  - **`changeTermSlug()`** (`src/app/admin/terms/actions.ts`) is a new action, separate from `renameTerm()` (which still only touches the display name — slug stays deliberately off-limits there). It **upserts** the current slug into `term_slug_history` keyed on `(kind, old_slug)` before updating `terms.slug` — upsert rather than plain insert because a slug could theoretically be renamed away and back later, and the newest departure should win. Admin UI (`src/app/admin/terms/page.tsx`) got a second, visually distinct "Change slug" form per term row (red button, tooltipped "Old URL will 301 here") next to the existing rename-name form.
  - **The redirect** lives in `src/lib/supabase/middleware.ts` (already runs on every request for session refresh) rather than a new middleware file — matches `/disciplines/[slug]` and `/disciplines/[slug]/[area]` (the only two URL shapes a discipline slug appears in), looks up `term_slug_history` by `old_slug`, and joins through `term_id` to the term's **current** slug rather than storing `old_slug → new_slug` directly — that means a slug renamed twice still resolves correctly on the very first hop, with no redirect chains, no matter how many times it's been changed since. A 301 (not 302) — a slug change is meant to be permanent, and this preserves whatever SEO weight the old URL built up.
  - Verified live end-to-end with a throwaway admin + a throwaway discipline term: changed its slug once via the real `/admin/terms` UI, confirmed `term_slug_history` had the right row and `terms.slug` updated; `curl -I` on both the old bare-discipline URL and the old discipline+area URL came back `301` to the new slug with the area segment correctly preserved; the new slug itself returned a normal `200` (no redirect loop); renamed a second time and confirmed **both** the original slug and the now-superseded first-rename slug immediately 301 straight to the newest one — proving the term_id join, not a follow-the-chain traversal, is what makes multi-hop renames resolve in one hop. Throwaway term, its history rows, and the throwaway admin all deleted after.
  - **Not deferred, actually out of scope:** area and coach slugs don't have their own history table (the spec's "term/area/coach slug" language is broader than what `0007_taxonomy.sql` actually built) — `term_slug_history` only ever covered terms. Area slugs are currently immutable in the admin UI (no rename control exists at all for `areas`) and coach slugs are coach-controlled but not editable post-creation either, so neither is a live gap today; worth a matching table if either becomes editable later.
- [x] **Phase 9e — Weekly SEO digest (29 Aug 2026):** wired for real, genuinely useful today, honest about the parts blocked on external setup nobody's done yet.
  - **`src/lib/search-console.ts`** — Google Search Console via a service account (`google-auth-library`'s `JWT`, not the full `googleapis` SDK — smaller install for one endpoint), guarded by `isGscConfigured` (needs both `GOOGLE_SERVICE_ACCOUNT_KEY` and `GSC_SITE_URL`) — same degrade-gracefully shape as `isStripeConfigured`/`isResendConfigured`. **Genuinely inert until the domain property is verified and the service account added as a user on it** (spec's own caveat) — this isn't a stub that needs rewriting later, it's the real integration, just correctly switched off because nobody's done the manual GCP/Search Console setup yet.
  - **`src/lib/seo-digest.ts`** builds every section the spec named: zero-result search split (supply gap vs vocabulary gap — computed from the site's **own** `search_events`, so this section works today with zero external dependencies), unmapped queries (a GSC query matching no known `term_aliases`/`terms.name`), near misses (GSC position 5-20), performance by URL pattern (buckets GSC page rows into `/coaches/[slug]`, `/disciplines/[slug]`, `/disciplines/[slug]/[area]`, `/riding-instructors/[area]`), 90-day prune candidates (`indexable_pages` rows eligible 90+ days with zero GSC impressions), and promotion candidates (a skill/attribute alias and a known area name appearing in the same GSC query — the spec's own example, "float loading Geelong"). Every GSC-dependent section degrades to one explanatory line, not an empty gap, when `isGscConfigured` is false.
  - **`src/app/api/cron/seo-digest/route.ts`** (added to `vercel.json`, `0 7 * * 1` — Monday mornings, `CRON_SECRET`-protected, same pattern as the other two cron routes) sends the digest via Resend to `SEO_DIGEST_EMAIL`; with either unset it logs the full digest text instead of sending, same "log it, don't send it" degrade as `notifyRidersOfClinic`, so the whole pipeline stays testable before Resend or GSC exist.
  - Verified live: seeded three real `search_events` rows (two zero-result Western searches near Broome WA, one with no discipline resolved) directly, hit `/api/cron/seo-digest`, confirmed the response and server log correctly split them into "2 supply gaps — Broome WA: 2" and "1 vocabulary gap", and confirmed the GSC section rendered its explanatory not-connected line rather than erroring or silently omitting. Build and lint both clean. Seeded rows deleted after.
  - **Deferred, not silently dropped:** the actual GCP project creation, service-account key generation, and Search Console domain-property verification are manual steps only the client/Alana can do (account creation and OAuth-style grants aren't something to do without the user) — tracked under Status/next steps as "verify the Search Console domain property on launch day", unchanged from before this phase. Once that's done, dropping the two env vars in is the entire remaining step — same "flip the keys, nothing else changes" shape as Stripe going live.
- [x] **Phase 9f — Drag-to-reorder for coach disciplines (29 Aug 2026):** replaced the "order ticked = sort_order" placeholder with a real drag-to-reorder UI, and fixed a bug along the way: that placeholder never actually worked. `formData.getAll()` returns checkbox values in **document order**, not click order — a checkbox's position in the DOM never moves no matter what order it's ticked in, so every coach's "primary" discipline was silently just whichever one sits first in the static list (Bridleless, always, if a coach teaches it), never their actual first choice.
  - **`DisciplinePicker`** (`src/app/dashboard/profile/profile-form.tsx`) separates the two concerns the old `TermCheckboxGroup` conflated: the checkbox row is now a plain controlled membership toggle (checked/unchecked, no `name` attribute — it doesn't submit anything itself), and a second `<ol>` below it renders only the checked disciplines as a reorderable list — native HTML5 drag-and-drop (no library) plus ↑/↓ buttons for keyboard/screen-reader access, since drag alone isn't operable that way. The list's React state (`order`, an array of ids) is the single source of truth, submitted via a run of `<input type="hidden" name="discipline">` tags rendered in that array's order — hidden inputs, unlike checkboxes, genuinely do serialize in DOM order, and their DOM order now comes from state, not from the static terms list. `saveProfile` (`src/app/dashboard/profile/actions.ts`) needed **no changes at all** — it already assigned `sort_order` from `formData.getAll("discipline")`'s array index, which was always correct; the bug was entirely upstream, in what order that array actually arrived in.
  - Skills and attributes keep the original `TermCheckboxGroup` unchanged — order never mattered there (`saveProfile` already hardcodes `sort_order: 0` for both), so a picker would be complexity with no payoff.
  - Verified live with a throwaway coach: ticked Western, Dressage, Show Jumping in that order (confirming the old bug — hidden-input order came out list-order, not click-order, until the drag step), moved Show Jumping to the top via the ↑ button, confirmed the hidden inputs reflected the new order client-side, saved, and read `coach_terms` straight back from the DB: `sort_order` 0/1/2 exactly matched Show Jumping/Western/Dressage. Caught two of my own test-script bugs during this, not app bugs: reading the reordered DOM synchronously right after `.click()` (React batches the re-render to the next microtask, so a same-tick read sees stale state — waiting ~500ms before reading fixed it) and, more consequentially, `document.querySelector('form')` grabbing the header's sign-out `<form>` instead of the profile form (there are 4 `<form>` elements on the page; the sign-out one happens to come first in document order) — every "save" was silently signing the test coach out and landing on the homepage instead. Fixed by scoping the query to `input[name="suburb"]).closest("form")`. Same standing lesson as every phase before this one: a green-looking test step (no thrown error, page didn't crash) still needs the actual DB row read back before it counts as verified.

## Mock coach data (added 29 Aug 2026)

## Mock coach data (added 29 Aug 2026)

50 mock coaches populate search/discipline/home pages for design & QA review, deliberately kept **out of Supabase** so they're trivial to remove later — nothing about them touches the DB, so there's no cleanup query to run and no risk of them being mistaken for real signups.

- Everything lives in `src/lib/mock-coaches.ts`: 25 real AU towns (with real lat/long, so radius search genuinely works against them via a plain haversine calc), cycled through 25 first names × 25 last names × the 11 real discipline slugs to procedurally generate 50 distinct-enough profiles (name, town, 1–2 disciplines, a templated headline/bio, a qualification, a tier). No clinics or testimonials on mock coaches — kept simple since clinics/testimonials were already proven out with real data in phases 3 and 6.
- **To turn off:** set `MOCK_COACHES_ENABLED = false` at the top of `src/lib/mock-coaches.ts`. **To remove entirely:** delete that file and the four small "Mock data merge" blocks that import from it — search the codebase for `mock-coaches` to find all of them (`src/app/page.tsx`, `src/app/search/page.tsx`, `src/app/disciplines/[slug]/page.tsx`, `src/app/coaches/[slug]/page.tsx`). Each merge block just concatenates the mock result array with the real Supabase one (and re-sorts by distance where relevant) — none of it touches `searchCoaches()` or the DB query layer itself.
- Mock coaches have no `coach_profiles` row, so their favourite button is correctly disabled (same as the static placeholder fallback) rather than erroring.
- **Stock photos (added 29 Aug 2026):** each mock coach now shows a real photo, chosen by their primary discipline — `PHOTO_BY_DISCIPLINE` in `src/lib/mock-coaches.ts` maps all 11 discipline slugs to one Unsplash photo ID each ([Unsplash License](https://unsplash.com/license) — free for commercial use). Per the brief: polished arena/competition shots for dressage, show jumping, eventing, working equitation, pony club and para-equestrian; warmer natural-connection shots for liberty, bridleless, natural horsemanship, western and campdrafting. Every candidate was actually downloaded and looked at (via the Read tool — the Browser pane wasn't rendering visually this session) before picking one, specifically to rule out any horse showing signs of stress (pinned ears, open mouth, hollow back, whites of the eyes) — none of the 11 chosen show that. The home page hero also now carries one of these (the dressage courtyard shot) instead of being text-only. Same "not worth `next/image` remote-pattern config yet" call already made for the profile-photo thumbnail in phase 3 applies here too — plain `<img>` / CSS `background-image`, eslint-disabled where needed.
- Verified live: 50 coaches show on `/search` with no filters, discipline + radius filtering narrows correctly (confirmed distance calc against a real town), a mock coach's detail page renders correctly, and no console/server errors anywhere in the merge.

## Running Supabase migrations (decided 28 Aug 2026)

**Preference: Claude Code runs migrations directly against the live DB, not via the Supabase SQL Editor.** No `supabase` CLI is installed in this environment and none is needed — a plain Node script using the `pg` package, run once and deleted, does the job:

- Direct connection (`db.<ref>.supabase.co:5432`) **will not work** from this machine/network — Supabase's direct host is IPv6-only here and there's no IPv6 route (`ENETUNREACH`). Don't waste time retrying it.
- Use the **session pooler** instead: `postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:5432/postgres`, with `ssl: { rejectUnauthorized: false }` in the `pg` client config (the pooler's cert chain isn't fully trusted by Node's default store, and `sslmode=require` in the URL string conflicts with an explicit `ssl` client option — don't combine them, pass `ssl` as a client option only).
- The pooler region is project-specific and isn't exposed anywhere obvious — **brute-force it**: loop over AWS regions (`us-east-1`, `us-east-2`, `us-west-1`, `us-west-2`, `ca-central-1`, `sa-east-1`, `eu-west-1/2/3`, `eu-central-1/2`, `eu-north-1`, `ap-southeast-1/2`, `ap-northeast-1/2`, `ap-south-1`) trying to connect; wrong region fails fast with `tenant/user ... not found`, right region connects immediately. This project's is `ap-northeast-1`. Once found, it's worth recording per-project rather than re-discovering each time — this project's full working connection string shape: `postgresql://postgres.hpvzjdrfnlkteuldnhyi:<password>@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres`.
- **Don't forget to actually run a migration file after writing it** — writing `0004_postcodes_latlong.sql` and forgetting to execute it (phase 4) caused a `column "lat" does not exist` failure that looked like a data problem. Run each new migration file the same way, immediately after creating it.
- **PostgREST embeds can be ambiguous** once a table has more than one FK path to another table — e.g. `coach_profiles` → `profiles` is ambiguous between the direct 1:1 FK and the `favourites` many-to-many bridge, and errors with `PGRST201`. Fix by naming the FK explicitly: `profiles!coach_profiles_id_fkey(name)` instead of `profiles(name)`. The error message lists the valid disambiguated names — use those verbatim.
- For anything touching search/RLS/embeds, don't just trust "no errors" — **run a real smoke test**: create a throwaway user via the GoTrue admin API (`POST {url}/auth/v1/admin/users` with the service-role key), insert real rows via the direct DB connection, exercise the actual code path (RPC call, or the live page in the Browser tool), then delete the user (cascades clean up everything else via FK `on delete cascade`). This caught both bugs above.
- `DATABASE_PASSWORD` lives in `.env` (gitignored) — the Postgres password, distinct from the anon/service-role API keys.
- Practical steps: `npm install --no-save pg`, write a small `.mjs` script that connects and runs each `supabase/migrations/*.sql` file (plus `supabase/seed.sql`) in order via `client.query(sql)`, run it, then delete the script and `npm uninstall pg` — keep it out of the committed dependency tree since the app itself never needs a raw Postgres driver (Supabase's JS SDK covers runtime queries).
- Verify with a quick follow-up query (e.g. `select table_name from information_schema.tables where table_schema='public'`) rather than assuming success from exit code alone.

## Partnership & setup decisions (updated 28 Aug 2026)

**The two people:** **Kim** — the business founder. This was her idea. She is a working riding instructor with an existing following and the deeper industry contact list, which makes her the face of all coach outreach. **Alana** — building the product. Rides and knows coaches too, ~1–5 hrs/week available. **Kim's available hours are still unknown and set the pace of the whole marketing plan** — open question to resolve.

Full decision doc: **["Before We Start"](https://claude.ai/code/artifact/5173be3a-497d-4de5-bbf4-8738f0f0d55d)** — seven decisions with options, suggested defaults and a blank column to fill in together. Nothing in the marketing plan can start until these are settled (no domain, no ABN, no Stripe = no outreach and no subscriptions).

**Nothing below is agreed yet.** These are the suggested defaults in the doc, not decisions:

1. **Structure — sole trader now, company later.** Kim gets an ABN + business name (~$47); Alana works under a written services agreement. Written trigger to form a Pty Ltd and issue Alana shares at **whichever comes first of month 8 (founding coaches converting to paid) or revenue passing $1,000/mo**. Avoid a general partnership — unlimited personal liability for each partner including the other's debts, wrong risk for a business connecting riders with instructors. Company costs (2026-27 ASIC): $636 to register, $342/yr annual review, plus ~$1,000–2,000/yr accountant.
2. **"30% of revenue" = money actually received less Stripe fees, refunds and chargebacks** ($2.86 on a $9.99 listing vs $3.00 gross). Paid monthly against an invoice from Alana's own ABN, with the Stripe payout report attached.
3. **Costs off the top**, then split. Whoever fronts money pre-revenue is repaid from first revenue before any split — one shared ledger. Joint sign-off on any spend over $200.
4. **30% tied to ongoing maintenance with a defined scope** (bugs, security, hosting, small content changes, ~4 hrs/mo) and a taper if Alana stops (10% for 12 months, then ends, unless already converted to shares). New features quoted separately.
5. **The business owns everything**, both admin on every account — domain, GitHub org, Vercel, Supabase, Stripe, Workspace, socials. Nothing in a personal account. Shared password manager from day one. Alana keeps the right to reuse generic components elsewhere; Kim gets a written handover clause so she is never stuck without a developer. **Alana's conversion right and Kim's handover right must be written at the same time, before the build is finished — neither is negotiable afterwards.**
6. **Who does what:** Kim leads coach recruitment, groups, press, events, and coach/content support. Alana leads build, hosting, SEO, technical faults and billing. Kim decides refunds, Alana executes them. One shared inbox at the domain, 2-business-day target.
7. **Exit and sale terms written now.** A revenue share normally *ends* at a sale — if Alana is to share in sale proceeds that has to be equity, not revenue share.

**Cost to the starting line: ~$1,270–2,870** (business name, agreement drafted/reviewed $500–1,500, domains, insurance $600–1,200/yr), plus the $1,148 marketing tranche one = **$2,420–4,020 to end of month six.**

**Get professionally checked:** an accountant on structure/tax and **whether superannuation applies to Alana's payments** (rules for contracts mainly for a person's labour); a solicitor on the agreement. GST registration not required below $75k turnover (~580 coaches).

## Marketing & go-to-market (researched 28 Aug 2026)

Full plan: **["The First 100 Coaches"](https://claude.ai/code/artifact/7e3f85dd-d0d9-400a-81dd-de56ccb2f8d3)** (Claude artifact). Headline findings:

- **Market size is the binding constraint.** Australia has roughly **3,000–6,000 people who coach riders for money** — estimated from Pony Club Australia (750+ clubs, ~30k members), EA's accredited register, and the unaccredited Western/liberty/natural-horsemanship long tail. Context: AHIC/IER (March 2025) put the non-racing sector at 1.03M horses, 700k+ people involved, $15.7bn. A market of ~3,000 named individuals **cannot be advertised into**.
- **Unit economics:** $10.74 net per coach/month (75/25 tier mix, after Stripe). Fixed costs ~$50/mo → **break-even at 5 paying coaches**. Base-case LTV $153 (7%/mo churn) → **max sustainable CAC $50**, target $30.
- **Paid ads cannot acquire coaches.** Meta can't target "is a riding coach"; reaching the ~500k AU equestrian-interest audience 5x at the $9.80 AU CPM benchmark costs ~$24,500 to find ~3,000 coaches, giving ~$272/coach best case. Do not run coach-acquisition ads. Google Search is worse — the intent doesn't exist.
- **Coach acquisition is manual and free:** personalised one-to-one outreach **from Kim** (working instructor, existing following, deepest contact list — her name on the messages is the whole strategy), with Alana taking a share of the list where she has contacts, a referral loop (refer 2 → 3 months free), coach-as-distributor share kits, Facebook group participation, print in saddleries/pony clubs, and equestrian press. Total tooling budget: $0 — no cold-email sequencer.
- **Rider traffic is a retention cost, not acquisition.** Coaches renew only if they got enquiries. SEO (already architected: SSR + 18.5k postcodes) is the main channel; paid rider traffic (Google Search local intent, ~$1.50–3.00 AU CPC) turns on month 7+ purely to keep coaches subscribed.
- **Budget: A$5,108 year one**, two gated tranches — $1,148 committed (months 1–6), $3,960 released only if gates pass (months 7–12).
- **Gates:** (1) week 12 — 25 complete published profiles in the beachhead region, under 12 = stop; (2) month 6 — 40+ coaches, 750+ rider sessions/mo, half of founders report an enquiry; (3) month 8 — founding-cohort paid conversion, 40%+ = release tranche two, under 25% = pivot to free listings + paid featured placement.
- **Launch shape:** nationally available day one, marketing concentrated on one beachhead region (the one Kim teaches in) and led on Western/liberty/natural horsemanship — the disciplines EA doesn't accredit, which makes the pitch concrete and doubles as a press angle.
- **Founding cohort:** first 25 get 12 months free, no card, price locked forever; coaches 26–100 get 6 months free with a **card required at signup** (trials with a card convert at 40–60% vs 15–25% without).
- **Hard rules:** never pre-create unclaimed profiles from scraped data (build it, then email for permission — reputation is the whole asset in a community this small); Spam Act 2003 requires consent + sender ID + working unsubscribe (live 30 days, honoured within 5 working days), with inferred consent only covering conspicuously published *business* addresses where the message is relevant to that role.
- **Biggest untested risk:** good coaches may already be full. If the first ten conversations say "I have a waitlist", the value proposition shifts from "get students" to "fill your clinics / be found when you move", and the $14.95 clinics tier becomes the main product rather than the upsell.
### Local SEO — the main long-term rider channel (added 28 Aug 2026)

Target queries are "riding instructor near me", "dressage coach Melbourne", "bridleless trainer Melbourne". Nobody in Australia owns these: most coaches have no site, EA's register sits behind a search form (generally not indexable), and Facebook posts don't rank. **But Google's 2024 core/helpful-content updates hit directories hard** — auto-generated town × category pages are treated as doorway spam. 18.5k postcodes × ~12 disciplines = 222,000 near-empty pages would sink the site. Benchmark from 2026 directory research: differentiated data-anchored pages index at ~49% vs ~43% for templated ones.

**Governing rule: only build a page when there is something real on it.** Concretely:

1. **Three page types.** Coach profiles (strongest — unique by definition); discipline × area pages (`/dressage-coaches/geelong`) **only where 3+ coaches exist**; one national page per discipline. ~400 real pages, not 222,000.
2. **Hard minimum of 3 coaches per area page** — below that, redirect to the parent area. Most important single rule.
3. **BUILD TASK — discipline search aliases.** Riders search "riding lessons"/"riding instructor" far more than "coach", and "bridleless trainer Melbourne" is a real low-competition query. The `disciplines` table needs a `search_aliases` field (Liberty → bridleless, at liberty, groundwork; Western → reining, barrel racing, campdraft; Dressage → flatwork) feeding page titles, H1s and body copy. **Add this before any area pages go live** — retrofitting is expensive.
4. **Hand-written intros (~150 words) on the top 20 area pages** — local clubs, grounds, strong disciplines. Templated intros are exactly what the 2024 update penalises.
5. **BUILD TASK — structured data.** `LocalBusiness`/`Person` schema on coach pages (address, geo, disciplines as `knowsAbout`); `ItemList` on area pages. Few hours' work, highest return after the pages themselves.
6. **Push every coach to create a free Google Business Profile** linked to their ECA page. The "near me" map pack only shows businesses with one — ECA can't rank there, individual coaches can, and each profile links back to us.
7. **Testimonials are the differentiator.** A profile with 8 rider testimonials beats one with a bio, for riders and for ranking. Make collection easy (shareable link + monthly coach email prompt).

**Upkeep:** Search Console from day one; review impressions **by URL pattern** monthly; prune or merge any page with zero impressions after 90 days. Expect 6–12 months before meaningful traffic — which is why it starts pre-launch. Google Search ads on "horse riding lessons [town]" (~$1.50–3.00 AU CPC) fill the gap from month 7.

- **Also not budgeted but launch-blocking:** public liability + professional indemnity insurance and terms disclaiming that you vet or accredit coaches (~$600–1,200/yr).

## Search & taxonomy build spec (28 Aug 2026)

Full spec: **["How Riders Find Us"](https://claude.ai/code/artifact/e11731ff-7c5f-4408-8e5c-e2fc9ea41a66)**. Written for the build (technical), unlike the marketing plan which is client-facing. Key decisions:

- **One `terms` table with a `kind` enum (`discipline` | `skill` | `attribute`)**, not three parallel tables — otherwise the alias layer, admin UI and search filter get built three times and drift. Existing `disciplines`/`coach_disciplines` fold into `terms`/`coach_terms` with an insert-select. Do it now, while only test coaches exist.
  - **discipline** = what they teach. The *only* kind that generates URLs (initially).
  - **skill** = what they fix — float loading, starting young horses, confidence building, OTTB retraining. Filters + page content, no URLs.
  - **attribute** = how they work — horses available for lessons, own arena, beginners welcome, NDIS registered.
- **`term_aliases`** with `unique(lower(alias))` — an alias belongs to exactly one term or matching is ambiguous. `source` column tracks seed/admin/search_console/onsite.
- **`term_suggestions`** maps discipline → suggested skills/attributes (curated seed; add a data-driven "other Western coaches also list…" row once 10+ coaches share a discipline). Cap the suggestion list at ~8.
- **`coach_terms.sort_order`** — lowest-ordered discipline is the coach's primary, used in page titles. No extra column, coach reorders by dragging.
- **`areas` table** (suburb/town/region/state, with centroid + hand-written intro), separate from the 18.5k `postcodes` which map into it. Riders search place names, not postcodes.
- **`indexable_pages` register** — nightly job recomputes which pages clear the 3-coach gate; sitemap, internal links and the prune report all read from it. Below the gate → 301 to the parent area page, never 404.
- **Four page types:** `/coaches/[slug]`, `/riding-instructors/[area]` (all disciplines — this catches the highest-volume "riding instructor near me" queries), `/disciplines/[slug]/[area]`, `/disciplines/[slug]`. `/search` is **noindex, follow** — faceted URLs are the classic directory crawl-budget disaster.
- **Multi-select search:** OR within a kind, AND across kinds → pass three arrays to `nearby_coaches` (`p_discipline_ids`, `p_skill_ids`, `p_attribute_ids`), not one array + match-all flag. Tie-break on match count after distance.
- **Aliases go in exactly four places:** title tag, one natural sentence of body copy, varied internal-link anchor text, and structured data (`knowsAbout` / `alternateName`). Never as a visible list — that is keyword stuffing.
- **Admin:** separate `admin_users` table + `is_admin()` — do NOT add `'admin'` to the `user_role` enum (it would mean touching the signup trigger and would stop an admin also being a coach). **Slug trap:** a term/area/coach slug in a live URL must be read-only once indexable; changes go through a deliberate action that writes `term_slug_history` and middleware 301s the old slug.
- **`search_events`** logs every on-site search (query, terms, location, result_count, click). Insert from the server action with the service-role client — **no anon insert policy**, that is an open spam endpoint. Zero-result searches split into supply gaps (recruiting targets) vs vocabulary gaps.
- **Weekly SEO digest** (Vercel Cron + `CRON_SECRET` → Resend email, not a dashboard): unmapped queries, zero-result splits, performance by URL pattern + 90-day prune list, near misses (position 5–20), and promotion candidates (skill terms searched *with a location* → flip `generates_pages = true`). GSC via a **service account** (`webmasters.readonly`), which must be added as a user on the property or it 403s. Limits: 2–3 day lag, 16-month retention, 25k rows/request, max 4 dimensions, 1,200 queries/100s.
- **Freshness:** coach pages static + on-demand `revalidatePath` on save (also revalidating their area pages); area pages ISR 1h + on-demand; sitemap `lastmod` from real `last_coach_change` (Google ignores fabricated ones). Clinics are the honest freshness engine since they expire by themselves. Monthly stale-profile nudge at 6 months untouched doubles as retention.
- **Verify the Search Console domain property (`sc-domain:`) on launch day**, even with 12 coaches. It only collects from verification and only holds 16 months.

## Status / next steps

- [ ] **Kim and Alana work through the seven decisions in ["Before We Start"](https://claude.ai/code/artifact/5173be3a-497d-4de5-bbf4-8738f0f0d55d)** — blocks ABN, domain, Stripe and therefore all marketing
- [ ] Find out how many hours a week Kim can give this — it sets the pace of the entire marketing plan
- [ ] Review 5 Claude Design directions and choose one (or graft favourite elements together)
- [ ] Lock in brand assets (logo, palette, type) from the chosen direction — apply to the app's design tokens once decided
- [ ] Firm up real hero copy (headline/subhead/CTA) instead of placeholder copy used in design exploration
- [ ] **Validate pricing before promising founding coaches a grandfathered rate.** $9.99 looks too cheap: EquiDirectory charges $80–$400/yr for a worse product, and a coach charging $70–$120/hr covers $9.99/mo with one extra lesson a year. Moving to $19/$29 roughly doubles net ARPU to $20.84 and base-case LTV to ~$298, which doubles what can be spent acquiring. Ask the first ten coaches what they'd pay without leading with a number.
- [ ] Pick the beachhead region (the one Alana rides in) and build the phase-zero list of 400–600 named coaches
- [x] Build the taxonomy layer per the spec below (`0007_taxonomy` migration + seed vocabulary) — supersedes the earlier "add search_aliases to disciplines" note — done in Phase 9 above
- [x] Add `areas` table and map postcodes into it — done in Phase 9 above
- [x] Rewrite `nearby_coaches` for multi-select (three arrays: disciplines / skills / attributes) — done in Phase 9 above
- [x] Profile editor: multi-select disciplines + suggested skills + attributes — done in Phase 9 above; drag-to-reorder done in Phase 9f
- [x] Log `search_events` from the search server action (service-role only, no anon policy) — done in Phase 9 above
- [x] `indexable_pages` register + nightly eligibility job + new page routes + sitemap — done in Phase 9b above
- [x] Structured data (`Person`/`LocalBusiness`, `ItemList`, `BreadcrumbList`) — done in Phase 9c above
- [x] `term_slug_history` 301s — done in Phase 9d above
- [x] `/admin` taxonomy screens — minimal terms/aliases admin built in Phase 9 above
- [x] Weekly SEO digest — done in Phase 9e above; still blocked on the manual step below
- [ ] **Verify the Search Console domain property on launch day**, add the service account as a user on it, then set `GOOGLE_SERVICE_ACCOUNT_KEY`/`GSC_SITE_URL`/`SEO_DIGEST_EMAIL`
- [ ] Decide the coach enquiry method on `/coaches/[slug]` (currently a placeholder — mailto vs. an in-app contact form)

## Working notes

- This project folder is the shared context for Claude across sessions — update this file as decisions get made (chosen direction, finalised pricing, MVP scope, tech stack) so future sessions don't have to re-derive them from scratch.
